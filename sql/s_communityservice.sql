CREATE TABLE CommunityService (
    csID int PRIMARY KEY NOT NULL AUTO_INCREMENT,
    Activity varchar(255),
    Organization varchar(255),
    AcademicYear varchar(255),
    Scope varchar(255),
    Description varchar(255)  
);