CREATE TABLE ConsultingExperience (
    ceID int PRIMARY KEY NOT NULL AUTO_INCREMENT,
    Activity varchar(255),
    Company varchar(255),
    AcademicYear varchar(255),
    Description varchar(255)  
);